{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "command": "bundle",
    "type": "shell",
    "args": ["exec"],
    "tasks": [
        {
            "label": "jekyll",
            "args": ["serve"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
